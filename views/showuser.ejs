<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <style>
        table,th,tr,td{
            border: 2px solid black;
        }
        table{
            background-color: pink;
        }
        #new{
            color: white;
      background-color: black;
      border:2px solid red;
      border-radius: 3px;

        }
    </style>
</head>
<body>
    <table>
        <tr>
           <th>No</th>
           <th>Id</th>
           <th>Username</th>
           <th>Email</th>
           <th>Edit</th>
           <th>Delete</th>
           
        </tr>
        <% for(user of users){ %>
            <tr>
              <td><%= i %></td>
              <td><%=user.id %></td>
              <td><%= user.username %></td>
              <td><%= user.email %></td>
              <td> <form  method="GET" action="/user/<%=user.id%>/edit">
                <button> Edit Username</button>
              </form></td>
              <td>
                <form method="post" action="/user/<%=user.id%>?_method=DELETE">
                    <button>Delete User</button> </form>
              </td>
              
            </tr>
            
            <%i++ %>
            <% } %>
            <br>
            <br>
            <form  method="http://localhost:8080/user/new" action="http://localhost:8080/user/new" >
                <button id="new">Create New user</button>
            </form>
            <br>
            <br>
    </table>

    
</body>
</html>